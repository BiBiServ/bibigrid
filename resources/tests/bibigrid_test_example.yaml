# remove _example in order to actually use this file
- infrastructure: # former mode.
  cloud: #credentials # name of clouds.yaml entry
  
  flavor: de.NBI small
  image: ^Ubuntu 22\.04 LTS \(.*\)$
  sshUser: ubuntu
  network: # network id

  snapshotImage: # name of a snapshot to create volume from. Volume needs to be deleted manually afterwards (optional)
