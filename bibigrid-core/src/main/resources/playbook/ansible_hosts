# The content of this file (ansible_hosts) should be generated by BiBiGrid during instance initialization.
# Depending on the used base image the ansible_user must be changed. The local ip address of each slave must
# inserted in the slave section

[master]
localhost ansible_connection=local

[slaves]
192.168.33.11 ansible_connection=ssh ansible_user=ubuntu
